export const SAMPLE_CODE = `#include <iostream>
#include <cstring>
using namespace std;

int main() {
    char buffer[10];
    gets(buffer);

    char password[] = "admin123";
    system("rm -rf /");

    return 0;
}`;
